const form=document.querySelector('#analysis-form'),message=document.querySelector('#analysis-message'),resultsDialog=document.querySelector('#analysis-results-dialog'),resultsGrid=document.querySelector('#analysis-results'),resultsSummary=document.querySelector('#results-summary'),logDialog=document.querySelector('#analysis-log-dialog'),logOutput=document.querySelector('#analysis-log');
const detail=async response=>{try{return(await response.json()).detail}catch{return null}};
if(form)form.onsubmit=async event=>{event.preventDefault();const button=form.querySelector('button[type="submit"]');button.disabled=true;message.textContent='Starting analysis…';const response=await fetch('/api/analysis/jobs',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({recording_id:document.querySelector('#analysis-recording').value,training_job_id:document.querySelector('#analysis-model').value,mode:document.querySelector('#analysis-mode').value,start_seconds:Number(document.querySelector('#analysis-start').value),end_seconds:Number(document.querySelector('#analysis-end').value),sample_interval_seconds:Number(document.querySelector('#analysis-interval').value),confidence_threshold:Number(document.querySelector('#analysis-confidence').value),crossing_window_seconds:Number(document.querySelector('#analysis-crossing-window').value)})});if(!response.ok){message.textContent=await detail(response)||'Could not start analysis';button.disabled=false;return}message.textContent='Analysis started. Detecting the ball and rim across time…';setTimeout(()=>location.reload(),1200)};
const syncMode=()=>{const full=document.querySelector('#analysis-mode').value==='full_game',recording=document.querySelector('#analysis-recording'),duration=Number(recording.selectedOptions[0]?.dataset.duration||0),button=form.querySelector('button[type="submit"]');button.textContent=full?'Analyze and create candidates':'Run calibration';if(full&&duration){document.querySelector('#analysis-start').value='0';document.querySelector('#analysis-end').value=duration.toFixed(1);document.querySelector('#analysis-interval').value='0.2';message.textContent='Full-game mode creates candidate highlights only. Existing reviewed decisions are protected.'}else message.textContent='Calibration explains crossings in a short range and does not create highlights.'};
document.querySelector('#analysis-mode')?.addEventListener('change',syncMode);document.querySelector('#analysis-recording')?.addEventListener('change',event=>{const duration=Number(event.target.selectedOptions[0]?.dataset.duration||0),end=document.querySelector('#analysis-end');if(document.querySelector('#analysis-mode').value==='full_game'&&duration)end.value=duration.toFixed(1);else if(duration&&Number(end.value)>duration)end.value=Math.min(30,duration).toFixed(1)});
document.querySelectorAll('[data-results]').forEach(button=>button.onclick=async()=>{const card=button.closest('[data-job-id]'),response=await fetch(`/api/analysis/jobs/${card.dataset.jobId}`),job=await response.json();resultsSummary.textContent=`${job.recording_title} · ${job.processed_frames} frames · ${job.detection_count} detections · ${job.candidate_count} crossings`;resultsGrid.replaceChildren();job.results.forEach(result=>{const crossing=result.explanation?.is_candidate,article=document.createElement('article');article.className=`result-card${crossing?' crossing':''}`;const image=document.createElement('img');image.src=result.image_url;image.loading='lazy';image.alt=`Detection preview at ${result.frame_time_seconds} seconds`;const meta=document.createElement('div');meta.className='result-meta';const text=document.createElement('div'),time=document.createElement('strong'),count=document.createElement('span');time.textContent=`${result.frame_time_seconds.toFixed(2)} sec`;count.textContent=`${result.detection_count} detection${result.detection_count===1?'':'s'}`;text.append(time,count);meta.append(text);if(result.explanation){const why=document.createElement('p');why.className=crossing?'crossing-reason':'frame-reason';why.textContent=crossing?`Made-basket crossing at ${result.explanation.event_time_seconds.toFixed(2)} sec · ${(result.explanation.confidence*100).toFixed(0)}% · ${result.explanation.reason}`:`${result.explanation.state.replaceAll('_',' ')} · ${result.explanation.reason}`;meta.append(why)}article.append(image,meta);resultsGrid.append(article)});resultsDialog.showModal()});
document.querySelectorAll('[data-log]').forEach(button=>button.onclick=async()=>{const card=button.closest('[data-job-id]'),response=await fetch(`/api/analysis/jobs/${card.dataset.jobId}/log`),body=await response.json();logOutput.textContent=body.log;logDialog.showModal()});
document.querySelectorAll('[data-cancel]').forEach(button=>button.onclick=async()=>{button.disabled=true;button.textContent='Stopping…';const card=button.closest('[data-job-id]'),response=await fetch(`/api/analysis/jobs/${card.dataset.jobId}/cancel`,{method:'POST'});if(!response.ok){button.textContent=await detail(response)||'Could not stop';return}setTimeout(()=>location.reload(),1200)});
document.querySelector('[data-close-results]')?.addEventListener('click',()=>resultsDialog.close());document.querySelector('[data-close-log]')?.addEventListener('click',()=>logDialog.close());
if(document.querySelector('[data-status="queued"],[data-status="running"]'))setTimeout(()=>location.reload(),5000);
